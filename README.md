# Airport API (DRF)

API service for managing airports, routes, flights, crews, airplanes and ticket orders.

## Tech
- Django REST Framework
- JWT auth (SimpleJWT)
- drf-spectacular (Swagger)
- Postgres (Docker) / SQLite (local)

## Quick start (local)
1. Create venv, install deps
2. Create `.env` from `.env.example`
3. Run migrations and server

More details will be added in final PR.
